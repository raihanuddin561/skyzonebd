import OrderDetailPage from '@/app/orders/[id]/page';

// Admin order detail page - reuses the customer order detail component
// The component already handles admin-specific features based on user role
export default OrderDetailPage;
